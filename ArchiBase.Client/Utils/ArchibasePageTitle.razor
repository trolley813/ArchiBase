@inject IConfiguration Configuration

<PageTitle>@Title — @(Configuration.GetValue<string>("SiteName", "ArchiBase"))</PageTitle>

@code
{
    [Parameter]
    public string Title { get; set; } = "";
}